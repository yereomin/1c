
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ ЗначениеЗаполнено(Запись.ТипИдентификатора) Тогда // новый
		Если ПустаяСтрока(Запись.ПочтовыйИдентификатор) ИЛИ СтрДлина(Запись.ПочтовыйИдентификатор) = 14 Тогда
			Запись.ТипИдентификатора = ПланыВидовХарактеристик.ВидыПочтовыхИдентификаторов.ПочтаРФ;
		КонецЕсли;	
	КонецЕсли;

	ЭтаФорма.ТолькоПросмотр = Запись.ВрученАдресату;
	
КонецПроцедуры
